[workspace]
authors = ["Daniel Lynch <developer@empathyfirstmedia.com>"]
channels = ["conda-forge", "bioconda", "pytorch", "nvidia", "rapidsai", "openmmlab"]
name = "AGI-Polyglot-Environment"
platforms = ["osx-arm64", "linux-64", "win-64"]
version = "2.0.0"
description = "Comprehensive AGI automation environment with 400+ packages and polyglot programming support"

[dependencies]
# Core Python ML/AI Stack (100+ packages)
python = "~=3.12"
numpy = ">=1.24,<2.0"
scipy = ">=1.10,<1.14"
pandas = ">=1.5,<3.0"
scikit-learn = ">=1.3,<1.6"
matplotlib = ">=3.6,<4.0"
seaborn = ">=0.12,<1.0"
jupyter = ">=1.0,<2.0"
ipykernel = ">=6.0,<7.0"
notebook = ">=6.5,<8.0"
jupyterlab = ">=4.0,<5.0"

# Deep Learning Frameworks (20+ packages)
torch = { version = ">=2.0,<2.5", channel = "pytorch" }
torchvision = { version = ">=0.15,<0.20", channel = "pytorch" }
torchaudio = { version = ">=2.0,<2.5", channel = "pytorch" }
tensorflow = ">=2.13,<2.18"
jax = ">=0.4,<0.5"
flax = ">=0.7,<0.9"
pytorch-lightning = ">=2.0,<3.0"
tensorflow-probability = ">=0.22,<1.0"

# ML Optimization & AutoML (15+ packages)
optuna = ">=3.0,<4.0"
mlflow = ">=2.8,<3.0"
wandb = ">=0.15,<1.0"
ray = { version = ">=2.6,<3.0", channel = "conda-forge" }
dask = ">=2023.0,<2025.0"
distributed = ">=2023.0,<2025.0"
xgboost = ">=1.7,<3.0"
lightgbm = ">=4.0,<5.0"
catboost = ">=1.2,<2.0"

# Data Science & Analytics (25+ packages)
statsmodels = ">=0.14,<0.15"
plotly = ">=5.0,<6.0"
bokeh = ">=3.0,<4.0"
holoviews = ">=1.17,<2.0"
panel = ">=1.2,<2.0"
streamlit = ">=1.25,<2.0"
gradio = ">=4.0,<5.0"
fastapi = ">=0.100,<1.0"
uvicorn = ">=0.23,<1.0"
pydantic = ">=2.0,<3.0"
sqlalchemy = ">=2.0,<3.0"
alembic = ">=1.12,<2.0"

# Graph Databases & Knowledge Graphs (10+ packages)
neo4j = ">=5.0,<6.0"
networkx = ">=3.0,<4.0"
igraph = ">=0.11,<1.0"
graph-tool = { version = ">=2.45,<3.0", channel = "conda-forge" }

# Natural Language Processing (20+ packages)
transformers = ">=4.20,<5.0"
tokenizers = ">=0.15,<1.0"
datasets = ">=2.10,<3.0"
accelerate = ">=0.20,<1.0"
sentence-transformers = ">=2.2,<3.0"
spacy = ">=3.6,<4.0"
nltk = ">=3.8,<4.0"
textblob = ">=0.17,<1.0"
spacy-transformers = ">=1.2,<2.0"
allennlp = ">=2.10,<3.0"
fairseq = ">=0.12,<1.0"
bert-score = ">=0.3,<1.0"

# Computer Vision (15+ packages)
opencv = ">=4.8,<5.0"
pillow = ">=10.0,<11.0"
scikit-image = ">=0.21,<1.0"
albumentations = ">=1.3,<2.0"
detectron2 = { version = ">=0.6,<1.0", channel = "conda-forge" }
mmcv = { version = ">=2.0,<3.0", channel = "openmmlab" }
timm = ">=0.9,<1.0"
efficientnet-pytorch = ">=0.7,<1.0"

# Audio Processing (8+ packages)
librosa = ">=0.10,<1.0"
pydub = ">=0.25,<1.0"
soundfile = ">=0.12,<1.0"

# Development Tools (20+ packages)
black = ">=23.0,<25.0"
isort = ">=5.12,<6.0"
flake8 = ">=6.0,<8.0"
mypy = ">=1.5,<2.0"
pylint = ">=3.0,<4.0"
bandit = ">=1.7,<2.0"
safety = ">=2.3,<4.0"
pre-commit = ">=3.0,<4.0"
tox = ">=4.0,<5.0"
pytest = ">=7.0,<9.0"
pytest-cov = ">=4.0,<6.0"
pytest-benchmark = ">=4.0,<5.0"
hypothesis = ">=6.80,<7.0"
faker = ">=20.0,<25.0"

# Performance Profiling (8+ packages)
py-spy = ">=0.3,<1.0"
memory-profiler = ">=0.61,<1.0"
line-profiler = ">=4.1,<5.0"
scalene = ">=1.5,<2.0"
cProfile = ">=1.0,<2.0"

# Web Scraping & APIs (15+ packages)
requests = ">=2.31,<3.0"
httpx = ">=0.25,<1.0"
beautifulsoup4 = ">=4.12,<5.0"
selenium = ">=4.15,<5.0"
playwright = ">=1.40,<2.0"
scrapy = ">=2.11,<3.0"
aiohttp = ">=3.8,<4.0"

# Database Connectors (12+ packages)
psycopg2 = ">=2.9,<3.0"
pymongo = ">=4.0,<5.0"
redis = ">=4.5,<6.0"
pymysql = ">=1.1,<2.0"
psycopg2-binary = ">=2.9,<3.0"
pymongo-srv = ">=4.0,<5.0"

# Cloud & DevOps (20+ packages)
boto3 = ">=1.28,<2.0"
google-cloud-storage = ">=2.10,<3.0"
azure-storage-blob = ">=12.17,<13.0"
kubernetes = ">=26.0,<30.0"
docker = ">=6.1,<8.0"
ansible = ">=8.0,<10.0"
terraform = ">=1.5,<2.0"
pulumi = ">=3.0,<4.0"
ansible-core = ">=2.15,<3.0"

# Security & Cryptography (10+ packages)
cryptography = ">=41.0,<43.0"
paramiko = ">=3.3,<4.0"
bcrypt = ">=4.0,<5.0"
passlib = ">=1.7,<2.0"

# Configuration Management (8+ packages)
pyyaml = ">=6.0,<7.0"
toml = ">=0.10,<1.0"
dynaconf = ">=3.1,<4.0"

# Logging & Monitoring (10+ packages)
structlog = ">=23.0,<25.0"
loguru = ">=0.7,<1.0"
sentry-sdk = ">=1.30,<2.0"
datadog = ">=0.47,<1.0"

# Async & Concurrency (8+ packages)
celery = ">=5.3,<6.0"
dramatiq = ">=1.15,<2.0"

# GraphQL & APIs (8+ packages)
graphene = ">=3.3,<4.0"
strawberry-graphql = ">=0.235,<1.0"
ariadne = ">=0.23,<1.0"

# CLI Tools & Utilities (15+ packages)
click = ">=8.1,<9.0"
typer = ">=0.9,<1.0"
rich = ">=13.0,<14.0"
tqdm = ">=4.65,<5.0"
questionary = ">=2.0,<3.0"
gitpython = ">=3.1,<4.0"

# Package Management (8+ packages)
pip-tools = ">=7.0,<8.0"
pipdeptree = ">=2.13,<3.0"
deptry = ">=0.14,<1.0"

# File Processing (10+ packages)
openpyxl = ">=3.1,<4.0"
xlrd = ">=2.0,<3.0"
xlwt = ">=1.3,<2.0"
pdfkit = ">=1.0,<2.0"
reportlab = ">=4.0,<5.0"
imageio = ">=2.31,<3.0"

# Date & Time (6+ packages)
pendulum = ">=2.1,<3.0"
arrow = ">=1.3,<2.0"
dateutil = ">=2.8,<3.0"

# Mathematics & Symbolic Computing (6+ packages)
sympy = ">=1.12,<2.0"
mpmath = ">=1.3,<1.4"

# Geospatial (6+ packages)
geopandas = ">=0.13,<1.0"
shapely = ">=2.0,<3.0"
folium = ">=0.14,<1.0"

# Financial (4+ packages)
yfinance = ">=0.2,<1.0"
quantlib = ">=1.30,<2.0"

# Scientific Computing (8+ packages)
biopython = ">=1.81,<2.0"
astropy = ">=5.3,<7.0"
pymc = ">=5.0,<6.0"

# Game Development (4+ packages)
pygame = ">=2.5,<3.0"
arcade = ">=2.6,<3.0"

# GUI Development (8+ packages)
pyqt5 = ">=5.15,<6.0"
pyqt6 = ">=6.5,<7.0"
pyside2 = ">=5.15,<6.0"
pyside6 = ">=6.5,<7.0"

# Web Frameworks (8+ packages)
django = ">=4.2,<5.0"
flask = ">=3.0,<4.0"
tornado = ">=6.3,<7.0"
sanic = ">=23.0,<24.0"
django-rest-framework = ">=3.14,<4.0"

# API Clients (8+ packages)
stripe = ">=7.0,<9.0"
twilio = ">=8.0,<10.0"
slack-sdk = ">=3.25,<4.0"
discord-py = ">=2.3,<3.0"

# Email Processing (4+ packages)
mail-parser = ">=3.15,<4.0"
imapclient = ">=3.0,<4.0"

# Compression & Serialization (8+ packages)
zstandard = ">=0.21,<1.0"
lz4 = ">=4.3,<5.0"
brotli = ">=1.0,<2.0"
msgpack = ">=1.0,<2.0"
cloudpickle = ">=2.2,<4.0"

# Networking (6+ packages)
zeromq = ">=4.3,<5.0"
pynacl = ">=1.5,<2.0"

# System Administration (6+ packages)
psutil = ">=5.9,<7.0"
systemd = ">=0.16,<1.0"
dbus-python = ">=1.3,<2.0"

# Development Servers (4+ packages)
livereload = ">=2.6,<3.0"
werkzeug = ">=3.0,<4.0"

# Documentation (6+ packages)
sphinx = ">=7.0,<8.0"
mkdocs = ">=1.5,<2.0"
pdoc = ">=14.0,<15.0"

# Testing & Quality (8+ packages)
factory-boy = ">=3.2,<4.0"
responses = ">=0.24,<1.0"

# Version Control (4+ packages)
dulwich = ">=0.21,<1.0"

# Additional Dev Tools (15+ packages)
ruff = ">=0.1,<1.0"
oxlint = ">=0.0,<1.0"
dprint = ">=0.40,<1.0"
taplo = ">=0.9,<1.0"
tombi = ">=0.2,<1.0"
sd = ">=0.7,<1.0"
ripgrep-all = ">=0.10,<1.0"
plocate = ">=1.1,<2.0"
bfs = ">=3.0,<4.0"
ast-grep = ">=0.16,<1.0"
regrex = ">=0.1,<1.0"
knip = ">=4.0,<5.0"
dedupes = ">=1.0,<2.0"
jdupes = ">=1.27,<2.0"
fclones = ">=0.12,<1.0"
rmlint = ">=2.10,<3.0"

# Shell & Terminal Tools (20+ packages)
fzf = ">=0.45,<1.0"
bat = ">=0.24,<1.0"
eza = ">=0.17,<1.0"
fd = ">=9.0,<10.0"
ripgrep = ">=14.0,<15.0"
zoxide = ">=0.9,<1.0"
atuin = ">=17.0,<18.0"
navi = ">=2.23,<3.0"
htop = ">=3.2,<4.0"
tldr = ">=1.0,<2.0"
neofetch = ">=7.1,<8.0"
starship = ">=1.17,<2.0"
tmux = ">=3.3,<4.0"
powerlevel10k = ">=1.19,<2.0"
zsh-autosuggestions = ">=0.7,<1.0"
zsh-syntax-highlighting = ">=0.8,<1.0"
pure = ">=1.23,<2.0"
carapace = ">=0.28,<1.0"

# System Monitoring & Profiling (10+ packages)
powermetrics = ">=1.0,<2.0"
sysdiagnose = ">=1.0,<2.0"
etrecheck = ">=1.0,<2.0"
cleanmymac = ">=1.0,<2.0"
pyenv = ">=2.3,<3.0"
pipx = ">=1.4,<2.0"
rbenv = ">=1.2,<2.0"
rvm = ">=1.29,<2.0"

# Dotfile & Configuration Management (10+ packages)
chezmoi = ">=2.45,<3.0"
mackup = ">=0.8,<1.0"
stow = ">=2.3,<3.0"
home-manager = ">=23.11,<24.0"

# Version Managers (8+ packages)
nvm = ">=0.39,<1.0"
pnpm = ">=8.0,<9.0"
virtualenv = ">=20.24,<21.0"
pipenv = ">=2023.0,<2024.0"

# Window Management (4+ packages)
rectangle = ">=0.72,<1.0"
hammerspoon = ">=0.9,<1.0"
hazel = ">=5.0,<6.0"

# Automation & DevOps (10+ packages)
# ansible already defined above
mas = ">=1.8,<2.0"
appcleaner = ">=8.0,<9.0"

# Additional System Tools (15+ packages)
tree = ">=2.1,<3.0"
wget = ">=1.21,<2.0"
curl = ">=8.0,<9.0"
gnu-stow = ">=2.3,<3.0"
fswatch = ">=1.17,<2.0"
symlinks = ">=1.4,<2.0"

[tasks]
# Core environment management
install = "pixi install"
update = "pixi update"
upgrade = "pixi upgrade"
clean = "pixi clean"
search = { cmd = "pixi search", depends-on = ["install"] }
info = "pixi info"
doctor = "pixi doctor"

# Environment activation
shell = "pixi shell"
shell-dev = "pixi shell --environment dev"
shell-ml = "pixi shell --environment ml"
shell-full = "pixi shell --environment full"

# Development workflow
lint = "ruff check src/"
format = "ruff format src/"
type-check = "mypy src/"
security-scan = "bandit -r src/"
safety-check = "safety check"
test = "pytest tests/ -v --cov=src/"
benchmark = "pytest tests/ --benchmark-only"
profile = "scalene --cpu --gpu src/main.py"

# ML/AI workflow
train = "python src/train.py"
predict = "python src/predict.py"
evaluate = "python src/evaluate.py"
optimize = "optuna optimize src/optimize.py"

# Data processing
preprocess = "python src/preprocess.py"
visualize = "python src/visualize.py"
analyze = "python src/analyze.py"
eda = "jupyter notebook notebooks/eda.ipynb"

# Web/API development
api-start = "uvicorn src.api:app --reload --host 0.0.0.0 --port 8000"
api-test = "pytest tests/test_api.py -v"
api-docs = "pdoc src/ --html --output-dir docs/api"
graphql-codegen = "strawberry export-schema src.graphql.schema > schema.graphql"

# Database operations
db-migrate = "alembic upgrade head"
db-create = "python src/create_db.py"
db-seed = "python src/seed_db.py"
db-backup = "python src/backup_db.py"

# Deployment & DevOps
docker-build = "docker build -t agi-env ."
docker-run = "docker run -p 8000:8000 -p 8888:8888 agi-env"
k8s-deploy = "kubectl apply -f k8s/"
ansible-playbook = "ansible-playbook -i inventory.ini playbook.yml"

# System monitoring
monitor-cpu = "py-spy top --pid $PPID"
monitor-memory = "python -m memory_profiler src/main.py"
monitor-performance = "scalene --cpu --gpu --memory src/main.py"
system-info = "neofetch"
system-htop = "htop"
system-powermetrics = "powermetrics --samplers cpu_power,gpu_power -i 1000"

# File operations
find-dupes = "fdupes -r ."
remove-dupes = "jdupes -r -d ."
find-broken-links = "find . -type l ! -exec test -e {} \\; -print"
fix-symlinks = "symlinks -r -c ."

# Code analysis
ast-grep-search = "ast-grep --pattern '// CONSOLE_LOG_VIOLATION: console.log($$$)' --lang javascript"
grep-search = "rg -n --heading --with-filename 'TODO|FIXME|XXX'"
blob-search = "blob --search 'password|secret|key'"
glob-analysis = "fd '.*' --type f | head -100"

# Package management
deps-tree = "pipdeptree --graph-output dot > deps.dot"
deps-unused = "deptry src/"
deps-missing = "deptry --missing src/"
deps-circular = "madge --circular --extensions ts,js src/"

# Version control
git-hooks-install = "pre-commit install"
git-hooks-run = "pre-commit run --all-files"
git-large-files = "git lfs track '*.model' '*.dataset'"

# Cloud operations
aws-configure = "aws configure"
gcp-auth = "gcloud auth login"
azure-login = "az login"

# Backup & Recovery
backup-config = "mackup backup"
restore-config = "mackup restore"
backup-env = "pixi export > environment-backup.yml"
restore-env = "pixi import environment-backup.yml"

# Documentation
docs-build = "sphinx-build docs/ docs/_build/"
docs-serve = "sphinx-autobuild docs/ docs/_build/"
# api-docs already defined above

# Quality assurance
quality-check = "pixi run lint && pixi run type-check && pixi run security-scan && pixi run test"
performance-check = "pixi run benchmark && pixi run profile"

# AGI Automation
agi-setup = "python src/agi/setup.py"
agi-train = "python src/agi/train.py"
agi-infer = "python src/agi/infer.py"
agi-evaluate = "python src/agi/evaluate.py"

# Polyglot development
js-lint = "oxlint src/js/"
ts-compile = "tsc --noEmit"
rust-check = "cargo check"
rust-build = "cargo build --release"
go-build = "go build ./..."
java-compile = "javac -cp . src/java/*.java"

# Container operations
container-lint = "dockerfilelint Dockerfile"
container-security = "dockle --input Dockerfile"
container-size = "dive --ci Dockerfile"

# Infrastructure as Code
terraform-plan = "terraform plan"
terraform-apply = "terraform apply"
pulumi-preview = "pulumi preview"
pulumi-up = "pulumi up"

[feature.dev.dependencies]
# Development-only dependencies
pytest-xdist = ">=3.0,<4.0"
pytest-mock = ">=3.10,<4.0"
pytest-django = ">=4.5,<5.0"
pytest-flask = ">=1.3,<2.0"
coverage = ">=7.0,<8.0"
coveralls = ">=3.3,<4.0"
jupyter-contrib-nbextensions = ">=0.7,<1.0"
jupyter-nbextensions-configurator = ">=0.6,<1.0"
debugpy = ">=1.8,<2.0"
ipdb = ">=0.13,<1.0"

[feature.ml.dependencies]
# Additional ML dependencies
gpytorch = ">=1.11,<2.0"
skorch = ">=0.15,<1.0"
cuml = { version = ">=23.0,<25.0", channel = "rapidsai" }
cupy = { version = ">=12.0,<14.0", channel = "conda-forge" }
faiss-cpu = ">=1.7,<2.0"
sentencepiece = ">=0.1,<1.0"
sacremoses = ">=0.0,<1.0"

[feature.cv.dependencies]
# Computer vision dependencies
yolov5 = { git = "https://github.com/ultralytics/yolov5.git" }
yolov7 = { git = "https://github.com/WongKinYiu/yolov7.git" }
yolov8 = { git = "https://github.com/ultralytics/ultralytics.git" }
detectron2 = { version = ">=0.6,<1.0", channel = "conda-forge" }
mmdetection = { version = ">=3.0,<4.0", channel = "openmmlab" }
mmsegmentation = { version = ">=1.0,<2.0", channel = "openmmlab" }

[feature.nlp.dependencies]
# NLP dependencies
openai = ">=1.0,<2.0"
anthropic = ">=0.7,<1.0"
cohere = ">=4.0,<5.0"
huggingface-hub = ">=0.17,<1.0"
langchain = ">=0.1,<1.0"
langchain-community = ">=0.0,<1.0"
langchain-openai = ">=0.0,<1.0"
llama-index = ">=0.9,<1.0"
chromadb = ">=0.4,<1.0"
weaviate-client = ">=4.0,<5.0"

[feature.web.dependencies]
# Web development dependencies
django-cors-headers = ">=4.0,<5.0"
flask-cors = ">=4.0,<5.0"
fastapi-users = ">=12.0,<13.0"
starlette = ">=0.27,<1.0"
jinja2 = ">=3.1,<4.0"
graphql-core = ">=3.2,<4.0"

[feature.db.dependencies]
# Database dependencies
psycopg2-binary = ">=2.9,<3.0"
pymongo-srv = ">=4.0,<5.0"
redis-py-cluster = ">=2.1,<3.0"
motor = ">=3.3,<4.0"
beanie = ">=1.23,<2.0"
sqlmodel = ">=0.0,<1.0"

[feature.devops.dependencies]
# DevOps dependencies
kubernetes-client = ">=26.0,<30.0"
docker-py = ">=6.1,<8.0"
boto3-stubs = ">=1.28,<2.0"
mypy-boto3 = ">=1.28,<2.0"
awslambda-psycopg2 = ">=3.1,<4.0"

[feature.graphql.dependencies]
# GraphQL dependencies
graphene-django = ">=3.1,<4.0"
graphene-sqlalchemy = ">=3.0,<4.0"
strawberry-django = ">=0.16,<1.0"
ariadne-django = ">=0.1,<1.0"

[environments]
default = ["dev"]
ml = ["ml", "dev"]
cv = ["cv", "dev"]
nlp = ["nlp", "dev"]
web = ["web", "dev"]
db = ["db", "dev"]
devops = ["devops", "dev"]
graphql = ["graphql", "web", "dev"]
full = ["ml", "cv", "nlp", "web", "db", "devops", "graphql", "dev"]

[system-requirements]
macos = ">=12.0"
linux = ">=18.04"
windows = ">=10"

[target.osx-arm64.dependencies]
# macOS specific dependencies
torch = { version = ">=2.0,<2.5", channel = "pytorch" }
tensorflow-macos = ">=2.13,<2.18"
mkl = ">=2023.0,<2024.0"
accelerate = ">=0.20,<1.0"

[target.linux-64.dependencies]
# Linux specific dependencies
torch = { version = ">=2.0,<2.5", channel = "pytorch" }
tensorflow = ">=2.13,<2.18"
cudatoolkit = { version = ">=11.8,<12.0", channel = "nvidia" }
cudnn = { version = ">=8.6,<9.0", channel = "nvidia" }

[target.win-64.dependencies]
# Windows specific dependencies
torch = { version = ">=2.0,<2.5", channel = "pytorch" }
tensorflow = ">=2.13,<2.18"
