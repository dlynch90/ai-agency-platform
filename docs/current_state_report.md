Inventory findings for /Users/daniellynch/Developer (Dec 21, 2025): `chezmoi status` shows drift (MM) for `~/.config/zed/settings.json`, `~/.config/zsh/.zprofile`, `~/.config/zsh/.zshenv`, `~/.config/zsh/.zshrc`, and `Developer/.mise.toml`. Argo CD dev env in `infra/argocd/environments/dev` has apps only for istio/kong/kyverno/datadog/external-secrets; event stack apps (Kafka/Temporal/Airflow/Prefect/Redis/Postgres/Neo4j/Qdrant/Prometheus/1Password Connect) are not present. `infra/vendor-sources.yaml` currently lists istio/kong/kyverno/datadog/external-secrets but not the event/data stack. `infra/helm-values` only contains `api-gateway` and `kong`. `mcp.json` already defines 24 MCP servers. `promptfoo.yaml` exists for LLM eval pipeline outputting to `reports/promptfoo/results.json`.