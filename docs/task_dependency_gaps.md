Learned repo patterns in /Users/daniellynch/Developer: `.mise.toml` is the canonical task runner with vendor-first tasks (setup:all -> setup:tools/ node/ python; repo.check -> repo.lint/typecheck/test; cache:clean uses `uv cache prune`, `pnpm store prune`, `mise prune`; cache:clean:workspace uses fd to remove .pytest_cache/.ruff_cache/.mypy_cache/.turbo and coverage files). Taskfile.yml references `pnpm clean:all` and `pnpm validate:all`, but no such scripts exist in package.json; validation should use `mise run repo.check` instead. Detected functional gaps: `apps/web/src/lib/kafka-client.ts` uses `kafkajs` but no package.json declares `kafkajs`; `apps/web/src/lib/temporal-client.ts` uses `@temporalio/client` but `apps/web/package.json` lacks @temporalio deps (pnpm workspace requires per-package deps). Root package.json workspaces use `app/*` while actual workspace is `apps/*` (pnpm uses pnpm-workspace.yaml but other tools may read workspaces).