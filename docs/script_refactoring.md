Gap-analysis inventory (12/21/2025) for /Users/daniellynch/Developer: fd found many custom .py/.sh scripts in repo root and /scripts (e.g., hf_auth_setup.sh, global-console-replacement.sh, scripts/db_hygiene/*.py/.sh, tooling/provenance/audit.py, tooling/sprawl/check.py). Taskfile.yml invokes custom Python for hygiene/audits (db_hygiene, system_hygiene, provenance, sprawl, audit_binaries, debug_recent_log_errors) and startup_evaluator. Recommendation: remove custom scripts and replace with vendor CLI workflows (mise tasks, uv cache, pnpm store prune, sqlite3/psql, gitleaks/trivy/grype/syft, rg/jq/yq) while keeping Taskfile as orchestrator calling mise run tasks. Vendor-only cache registry should use uv config (tool.uv.cache-keys) and mise cache prune/mise prune; avoid custom cache scripts.