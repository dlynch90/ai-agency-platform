Added vendor-only GitOps scaffolding for local k8s stack in /Users/daniellynch/Developer: new Argo CD apps under `infra/argocd/environments/dev/` for `onepassword-connect`, `strimzi-kafka-operator`, `temporal`, `airflow`, `prefect` (placeholders), `cloudnative-pg`, `redis`, `neo4j`, `qdrant`, and `kube-prometheus-stack`. Added minimal Helm values placeholders under `infra/helm-values/{onepassword-connect,strimzi,temporal,airflow,prefect,cloudnative-pg,redis,neo4j,qdrant,prometheus}/values.yaml` (Airflow includes required Argo CD overrides). Updated `infra/vendor-sources.yaml` to register vendor sources and repo URLs for 1Password Connect, Strimzi, Temporal, Airflow, Prefect, CloudNativePG, Redis, Neo4j, Qdrant, and Prometheus stack with pinned versions where known.