Vendor-only migration executed in /Users/daniellynch/Developer (12/21/2025): removed custom automation directories `/Users/daniellynch/Developer/scripts` and `/Users/daniellynch/Developer/tooling`, plus root scripts `hf_auth_setup.sh` and `global-console-replacement.sh`. Taskfile.yml updated to eliminate custom script invocations: startup validation now runs `mise doctor`, `uv --version`, `gh --version`, `node --version`, `python --version`; hygiene:sqlite uses `fd` + `sqlite3` VACUUM; hygiene:system uses `du` + `find`; audit tasks use `gitleaks` + `trivy`; audit:binaries uses `mise doctor`/`mise ls --prunable`; logs:errors uses `rg`. Hygiene caches now calls `mise run cache:clean`. Added Taskfile `tdd:loop` using `watchexec` to run `pnpm test` and `uv run pytest -q`. Launchd plist moved to `config/launchd/com.developer.db-hygiene.plist` and now runs `task hygiene:sqlite`.

Mise tasks updated in `/Users/daniellynch/Developer/.mise.toml`: cache tasks now include `pnpm store prune` and show pnpm store size; removed perf tasks that referenced scripts; `ai:test` now `ollama list`; added `eval:pipeline` depends on `config:all`, `security:audit`, `dev:test`, `dev:lint`. Added `tool.uv.cache-keys` to `/Users/daniellynch/Developer/pyproject.toml` to standardize cache registry. README.md and QUICK_START.md updated to remove script references and use `jq` for JSON validation.