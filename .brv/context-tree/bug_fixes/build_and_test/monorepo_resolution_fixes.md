Rebuild + test fixes (Dec 21, 2025) in /Users/daniellynch/Developer: cleared caches (`rm -rf node_modules .turbo`, fd cleanup of node_modules/.turbo/.next/dist/build/__pycache__/.pytest_cache), pruned pnpm store, forced `uv cache prune --force`, ran `mise run setup:all`, `pnpm -w build`, `pnpm -w test`, `uv sync --all-extras`, and `task health`. Build fixes: added root deps `pino` + `dotenv` via `pnpm -w add pino dotenv` to avoid workspace TypeScript resolution failures; ran `pnpm -C packages/@vendor/catalog install` to ensure prisma CLI for vendor-catalog build/tests. Test fixes: renamed `apps/web/src/__tests__/hooks/useGenerativeAI.test.ts` -> `.tsx`; updated tests to await message length and use generateResponse for autoconversation. Hook fix in `apps/web/src/hooks/use-generative-ai.ts`: `sendMessageMutation` now accepts `conversation` param; added `getActiveConversation`, `sendMessage`, and updated `generateResponse` to use conversation explicitly; logging uses `baseConversation.id`. Result: `pnpm -w test` passes (warnings about WebSocket not connected + missing vendor-catalog .env are non-fatal).