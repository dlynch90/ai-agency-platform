# ============================================================================
# AI AGENCY PLATFORM - ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# ============================================================================

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
NODE_ENV=development
APP_NAME=ai-agency-platform
APP_VERSION=1.0.0
APP_PORT=${APP_PORT:-8080}
APP_HOST=${APP_HOST:-0.0.0.0}
APP_URL=${APP_URL:-http://localhost:8080}
API_VERSION=v1
LOG_LEVEL=${LOG_LEVEL:-info}
DEBUG=${DEBUG:-false}

# ============================================================================
# AUTHENTICATION - CLERK
# ============================================================================
CLERK_PUBLISHABLE_KEY=${CLERK_PUBLISHABLE_KEY:-}
CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-}
CLERK_WEBHOOK_SECRET=${CLERK_WEBHOOK_SECRET:-}
CLERK_JWT_KEY=${CLERK_JWT_KEY:-}
CLERK_FRONTEND_API=${CLERK_FRONTEND_API:-}
CLERK_API_URL=https://api.clerk.dev

# ============================================================================
# DATABASE - SUPABASE
# ============================================================================
SUPABASE_URL=${SUPABASE_URL:-}
SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY:-}
SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY:-}
SUPABASE_JWT_SECRET=${SUPABASE_JWT_SECRET:-}
SUPABASE_DB_URL=${SUPABASE_DB_URL:-}
SUPABASE_STORAGE_URL=${SUPABASE_STORAGE_URL:-}
SUPABASE_REALTIME_URL=${SUPABASE_REALTIME_URL:-}

# ============================================================================
# DATABASE - POSTGRESQL
# ============================================================================
POSTGRES_HOST=${POSTGRES_HOST:-localhost}
POSTGRES_PORT=${POSTGRES_PORT:-5432}
POSTGRES_USER=${POSTGRES_USER:-postgres}
POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-}
POSTGRES_DB=${POSTGRES_DB:-ai_agency}
POSTGRES_SSL=${POSTGRES_SSL:-false}
POSTGRES_MAX_CONNECTIONS=${POSTGRES_MAX_CONNECTIONS:-20}
POSTGRES_POOL_MIN=${POSTGRES_POOL_MIN:-2}
POSTGRES_POOL_MAX=${POSTGRES_POOL_MAX:-10}
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# ============================================================================
# DATABASE - REDIS
# ============================================================================
REDIS_HOST=${REDIS_HOST:-localhost}
REDIS_PORT=${REDIS_PORT:-6379}
REDIS_PASSWORD=${REDIS_PASSWORD:-}
REDIS_DB=${REDIS_DB:-0}
REDIS_TLS=${REDIS_TLS:-false}
REDIS_MAX_CONNECTIONS=${REDIS_MAX_CONNECTIONS:-20}
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

# ============================================================================
# DATABASE - NEO4J (Knowledge Graph)
# ============================================================================
NEO4J_HOST=${NEO4J_HOST:-localhost}
NEO4J_BOLT_PORT=${NEO4J_BOLT_PORT:-7687}
NEO4J_HTTP_PORT=${NEO4J_HTTP_PORT:-7474}
NEO4J_USER=${NEO4J_USER:-neo4j}
NEO4J_PASSWORD=${NEO4J_PASSWORD:-}
NEO4J_DATABASE=${NEO4J_DATABASE:-neo4j}
NEO4J_MAX_CONNECTIONS=${NEO4J_MAX_CONNECTIONS:-10}
NEO4J_URI=bolt://${NEO4J_HOST}:${NEO4J_BOLT_PORT}

# ============================================================================
# VECTOR DATABASE - QDRANT
# ============================================================================
QDRANT_HOST=${QDRANT_HOST:-localhost}
QDRANT_PORT=${QDRANT_PORT:-6333}
QDRANT_GRPC_PORT=${QDRANT_GRPC_PORT:-6334}
QDRANT_API_KEY=${QDRANT_API_KEY:-}
QDRANT_COLLECTION=${QDRANT_COLLECTION:-ai_agency_vectors}
QDRANT_URL=http://${QDRANT_HOST}:${QDRANT_PORT}

# ============================================================================
# MESSAGE QUEUE - KAFKA
# ============================================================================
KAFKA_BROKERS=${KAFKA_BROKERS:-localhost:9092}
KAFKA_CLIENT_ID=${KAFKA_CLIENT_ID:-ai-agency}
KAFKA_GROUP_ID=${KAFKA_GROUP_ID:-ai-agency-group}
KAFKA_SSL=${KAFKA_SSL:-false}
KAFKA_SASL_MECHANISM=${KAFKA_SASL_MECHANISM:-}
KAFKA_SASL_USERNAME=${KAFKA_SASL_USERNAME:-}
KAFKA_SASL_PASSWORD=${KAFKA_SASL_PASSWORD:-}
KAFKA_MAX_CONNECTIONS=${KAFKA_MAX_CONNECTIONS:-10}

# ============================================================================
# MESSAGE QUEUE - RABBITMQ
# ============================================================================
RABBITMQ_HOST=${RABBITMQ_HOST:-localhost}
RABBITMQ_PORT=${RABBITMQ_PORT:-5672}
RABBITMQ_USER=${RABBITMQ_USER:-guest}
RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD:-guest}
RABBITMQ_VHOST=${RABBITMQ_VHOST:-/}
RABBITMQ_MAX_CONNECTIONS=${RABBITMQ_MAX_CONNECTIONS:-15}
RABBITMQ_URL=amqp://${RABBITMQ_USER}:${RABBITMQ_PASSWORD}@${RABBITMQ_HOST}:${RABBITMQ_PORT}/${RABBITMQ_VHOST}

# ============================================================================
# WORKFLOW ENGINE - TEMPORAL
# ============================================================================
TEMPORAL_ADDRESS=${TEMPORAL_ADDRESS:-localhost:7233}
TEMPORAL_NAMESPACE=${TEMPORAL_NAMESPACE:-default}
TEMPORAL_TASK_QUEUE=${TEMPORAL_TASK_QUEUE:-ai-agency-tasks}
TEMPORAL_WORKER_COUNT=${TEMPORAL_WORKER_COUNT:-4}
TEMPORAL_UI_PORT=${TEMPORAL_UI_PORT:-8080}

# ============================================================================
# API GATEWAY - KONG
# ============================================================================
KONG_PROXY_URL=${KONG_PROXY_URL:-http://localhost:8000}
KONG_ADMIN_URL=${KONG_ADMIN_URL:-http://localhost:8001}
KONG_PROXY_SSL_URL=${KONG_PROXY_SSL_URL:-https://localhost:8443}
KONG_ADMIN_SSL_URL=${KONG_ADMIN_SSL_URL:-https://localhost:8444}
KONG_DATABASE=${KONG_DATABASE:-off}

# ============================================================================
# GRAPHQL CONFIGURATION
# ============================================================================
GRAPHQL_ENDPOINT=${GRAPHQL_ENDPOINT:-/graphql}
GRAPHQL_PORT=${GRAPHQL_PORT:-4000}
GRAPHQL_PLAYGROUND=${GRAPHQL_PLAYGROUND:-true}
GRAPHQL_INTROSPECTION=${GRAPHQL_INTROSPECTION:-true}
GRAPHQL_DEBUG=${GRAPHQL_DEBUG:-false}
GRAPHQL_DEPTH_LIMIT=${GRAPHQL_DEPTH_LIMIT:-10}
GRAPHQL_COMPLEXITY_LIMIT=${GRAPHQL_COMPLEXITY_LIMIT:-1000}

# ============================================================================
# AI/ML - OPENAI
# ============================================================================
OPENAI_API_KEY=${OPENAI_API_KEY:-}
OPENAI_ORG_ID=${OPENAI_ORG_ID:-}
OPENAI_MODEL=${OPENAI_MODEL:-gpt-4}
OPENAI_EMBEDDING_MODEL=${OPENAI_EMBEDDING_MODEL:-text-embedding-3-small}
OPENAI_MAX_TOKENS=${OPENAI_MAX_TOKENS:-4096}
OPENAI_TEMPERATURE=${OPENAI_TEMPERATURE:-0.7}

# ============================================================================
# AI/ML - ANTHROPIC
# ============================================================================
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
ANTHROPIC_MODEL=${ANTHROPIC_MODEL:-claude-3-opus-20240229}
ANTHROPIC_MAX_TOKENS=${ANTHROPIC_MAX_TOKENS:-4096}

# ============================================================================
# AI/ML - HUGGINGFACE
# ============================================================================
HUGGINGFACE_API_KEY=${HUGGINGFACE_API_KEY:-}
HUGGINGFACE_MODEL=${HUGGINGFACE_MODEL:-}
HUGGINGFACE_ENDPOINT=${HUGGINGFACE_ENDPOINT:-}

# ============================================================================
# AI/ML - OLLAMA (Local LLM)
# ============================================================================
OLLAMA_HOST=${OLLAMA_HOST:-localhost}
OLLAMA_PORT=${OLLAMA_PORT:-11434}
OLLAMA_MODEL=${OLLAMA_MODEL:-llama3}
OLLAMA_URL=http://${OLLAMA_HOST}:${OLLAMA_PORT}

# ============================================================================
# AI/ML - LITELLM PROXY
# ============================================================================
LITELLM_HOST=${LITELLM_HOST:-localhost}
LITELLM_PORT=${LITELLM_PORT:-4000}
LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY:-}
LITELLM_URL=http://${LITELLM_HOST}:${LITELLM_PORT}

# ============================================================================
# ML TRACKING - MLFLOW
# ============================================================================
MLFLOW_TRACKING_URI=${MLFLOW_TRACKING_URI:-http://localhost:5000}
MLFLOW_EXPERIMENT_NAME=${MLFLOW_EXPERIMENT_NAME:-ai-agency}
MLFLOW_S3_ENDPOINT_URL=${MLFLOW_S3_ENDPOINT_URL:-}
MLFLOW_ARTIFACT_ROOT=${MLFLOW_ARTIFACT_ROOT:-./mlruns}

# ============================================================================
# SEARCH - ELASTICSEARCH
# ============================================================================
ELASTICSEARCH_HOST=${ELASTICSEARCH_HOST:-localhost}
ELASTICSEARCH_PORT=${ELASTICSEARCH_PORT:-9200}
ELASTICSEARCH_USER=${ELASTICSEARCH_USER:-}
ELASTICSEARCH_PASSWORD=${ELASTICSEARCH_PASSWORD:-}
ELASTICSEARCH_INDEX=${ELASTICSEARCH_INDEX:-ai_agency}
ELASTICSEARCH_URL=http://${ELASTICSEARCH_HOST}:${ELASTICSEARCH_PORT}

# ============================================================================
# MONITORING - PROMETHEUS & GRAFANA
# ============================================================================
PROMETHEUS_HOST=${PROMETHEUS_HOST:-localhost}
PROMETHEUS_PORT=${PROMETHEUS_PORT:-9090}
PROMETHEUS_URL=http://${PROMETHEUS_HOST}:${PROMETHEUS_PORT}
GRAFANA_HOST=${GRAFANA_HOST:-localhost}
GRAFANA_PORT=${GRAFANA_PORT:-3000}
GRAFANA_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
GRAFANA_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:-}
GRAFANA_URL=http://${GRAFANA_HOST}:${GRAFANA_PORT}

# ============================================================================
# LOGGING - DATADOG
# ============================================================================
DD_API_KEY=${DD_API_KEY:-}
DD_APP_KEY=${DD_APP_KEY:-}
DD_SITE=${DD_SITE:-datadoghq.com}
DD_ENV=${DD_ENV:-development}
DD_SERVICE=${DD_SERVICE:-ai-agency}
DD_LOGS_ENABLED=${DD_LOGS_ENABLED:-true}
DD_APM_ENABLED=${DD_APM_ENABLED:-true}

# ============================================================================
# STORAGE - AWS S3 / MINIO
# ============================================================================
AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-}
AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-}
AWS_REGION=${AWS_REGION:-us-east-1}
AWS_S3_BUCKET=${AWS_S3_BUCKET:-ai-agency-storage}
AWS_S3_ENDPOINT=${AWS_S3_ENDPOINT:-}
S3_USE_PATH_STYLE=${S3_USE_PATH_STYLE:-false}

# ============================================================================
# EMAIL - SENDGRID
# ============================================================================
SENDGRID_API_KEY=${SENDGRID_API_KEY:-}
SENDGRID_FROM_EMAIL=${SENDGRID_FROM_EMAIL:-}
SENDGRID_FROM_NAME=${SENDGRID_FROM_NAME:-AI Agency}

# ============================================================================
# EMAIL - RESEND
# ============================================================================
RESEND_API_KEY=${RESEND_API_KEY:-}
RESEND_FROM_EMAIL=${RESEND_FROM_EMAIL:-}

# ============================================================================
# PAYMENTS - STRIPE
# ============================================================================
STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY:-}
STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY:-}
STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET:-}
STRIPE_PRICE_ID=${STRIPE_PRICE_ID:-}

# ============================================================================
# CRM - INTEGRATIONS
# ============================================================================
HUBSPOT_API_KEY=${HUBSPOT_API_KEY:-}
HUBSPOT_PORTAL_ID=${HUBSPOT_PORTAL_ID:-}
SALESFORCE_CLIENT_ID=${SALESFORCE_CLIENT_ID:-}
SALESFORCE_CLIENT_SECRET=${SALESFORCE_CLIENT_SECRET:-}
SALESFORCE_INSTANCE_URL=${SALESFORCE_INSTANCE_URL:-}

# ============================================================================
# MARKETING - INTEGRATIONS
# ============================================================================
MAILCHIMP_API_KEY=${MAILCHIMP_API_KEY:-}
MAILCHIMP_LIST_ID=${MAILCHIMP_LIST_ID:-}
GA_TRACKING_ID=${GA_TRACKING_ID:-}
SEGMENT_WRITE_KEY=${SEGMENT_WRITE_KEY:-}
MIXPANEL_TOKEN=${MIXPANEL_TOKEN:-}

# ============================================================================
# NOTION INTEGRATION
# ============================================================================
NOTION_API_KEY=${NOTION_API_KEY:-}
NOTION_DATABASE_ID=${NOTION_DATABASE_ID:-}
NOTION_WORKSPACE_ID=${NOTION_WORKSPACE_ID:-}

# ============================================================================
# EXCEL / SPREADSHEET INTEGRATION
# ============================================================================
GOOGLE_SHEETS_API_KEY=${GOOGLE_SHEETS_API_KEY:-}
GOOGLE_SHEETS_CLIENT_ID=${GOOGLE_SHEETS_CLIENT_ID:-}
GOOGLE_SHEETS_CLIENT_SECRET=${GOOGLE_SHEETS_CLIENT_SECRET:-}
GOOGLE_SHEETS_REFRESH_TOKEN=${GOOGLE_SHEETS_REFRESH_TOKEN:-}

# ============================================================================
# VERSION CONTROL - GITHUB
# ============================================================================
GITHUB_TOKEN=${GITHUB_TOKEN:-}
GITHUB_WEBHOOK_SECRET=${GITHUB_WEBHOOK_SECRET:-}
GITHUB_APP_ID=${GITHUB_APP_ID:-}
GITHUB_APP_PRIVATE_KEY=${GITHUB_APP_PRIVATE_KEY:-}
GITHUB_ORG=${GITHUB_ORG:-}

# ============================================================================
# COMMUNICATION - SLACK
# ============================================================================
SLACK_BOT_TOKEN=${SLACK_BOT_TOKEN:-}
SLACK_SIGNING_SECRET=${SLACK_SIGNING_SECRET:-}
SLACK_APP_TOKEN=${SLACK_APP_TOKEN:-}
SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL:-}

# ============================================================================
# COMMUNICATION - DISCORD
# ============================================================================
DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN:-}
DISCORD_CLIENT_ID=${DISCORD_CLIENT_ID:-}
DISCORD_CLIENT_SECRET=${DISCORD_CLIENT_SECRET:-}
DISCORD_WEBHOOK_URL=${DISCORD_WEBHOOK_URL:-}

# ============================================================================
# SECURITY
# ============================================================================
JWT_SECRET=${JWT_SECRET:-}
JWT_EXPIRY=${JWT_EXPIRY:-86400}
JWT_REFRESH_EXPIRY=${JWT_REFRESH_EXPIRY:-604800}
ENCRYPTION_KEY=${ENCRYPTION_KEY:-}
CORS_ORIGINS=${CORS_ORIGINS:-http://localhost:3000,http://localhost:8080}
RATE_LIMIT_MAX=${RATE_LIMIT_MAX:-100}
RATE_LIMIT_WINDOW=${RATE_LIMIT_WINDOW:-60000}

# ============================================================================
# FEATURE FLAGS
# ============================================================================
FEATURE_MARKETING_MODULE=${FEATURE_MARKETING_MODULE:-true}
FEATURE_WEB_DESIGN_MODULE=${FEATURE_WEB_DESIGN_MODULE:-true}
FEATURE_CRM_MODULE=${FEATURE_CRM_MODULE:-true}
FEATURE_EXCEL_MODULE=${FEATURE_EXCEL_MODULE:-true}
FEATURE_NOTION_MODULE=${FEATURE_NOTION_MODULE:-true}
FEATURE_AI_AGENTS=${FEATURE_AI_AGENTS:-true}
FEATURE_ANALYTICS=${FEATURE_ANALYTICS:-true}
FEATURE_REALTIME=${FEATURE_REALTIME:-true}

# ============================================================================
# TESTING
# ============================================================================
TEST_DATABASE_URL=${TEST_DATABASE_URL:-}
TEST_REDIS_URL=${TEST_REDIS_URL:-}
E2E_BASE_URL=${E2E_BASE_URL:-http://localhost:3000}
PLAYWRIGHT_HEADLESS=${PLAYWRIGHT_HEADLESS:-true}
JEST_TIMEOUT=${JEST_TIMEOUT:-30000}

# ============================================================================
# BINOMIAL DISTRIBUTION / STATISTICAL CONFIG
# ============================================================================
STAT_BINOMIAL_N=${STAT_BINOMIAL_N:-5}
STAT_BINOMIAL_P=${STAT_BINOMIAL_P:-0.10}
STAT_CONFIDENCE_LEVEL=${STAT_CONFIDENCE_LEVEL:-0.95}
STAT_TAIL_THRESHOLD=${STAT_TAIL_THRESHOLD:-0.05}

# ============================================================================
# FINITE ELEMENT ANALYSIS CONFIG
# ============================================================================
FEA_MESH_DENSITY=${FEA_MESH_DENSITY:-100}
FEA_TOLERANCE=${FEA_TOLERANCE:-1e-6}
FEA_MAX_ITERATIONS=${FEA_MAX_ITERATIONS:-1000}
FEA_SOLVER_TYPE=${FEA_SOLVER_TYPE:-direct}

# ============================================================================
# PDE SOLVER CONFIG
# ============================================================================
PDE_GRID_SIZE=${PDE_GRID_SIZE:-50}
PDE_TIME_STEPS=${PDE_TIME_STEPS:-1000}
PDE_BOUNDARY_TYPE=${PDE_BOUNDARY_TYPE:-dirichlet}
PDE_METHOD=${PDE_METHOD:-finite_difference}
