{{- if .Values.secrets.enabled -}}
{{- $fullName := include "agi-environment.fullname" . -}}
{{- $labels := include "agi-environment.labels" . -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $fullName }}-secrets
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    {{- $labels | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets.data }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
---
# ExternalSecret template (for use with external-secrets operator)
{{- if .Values.secrets.enabled }}
{{- $fullName := include "agi-environment.fullname" . -}}
{{- $labels := include "agi-environment.labels" . -}}
# Uncomment to use with external-secrets operator
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: {{ $fullName }}-external-secrets
#   namespace: {{ .Values.global.namespace | default .Release.Namespace }}
#   labels:
#     {{- $labels | nindent 4 }}
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: vault-backend
#   target:
#     name: {{ $fullName }}-external-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: database-password
#       remoteRef:
#         key: secret/data/{{ $fullName }}
#         property: database-password
{{- end }}
